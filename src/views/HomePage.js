import React from 'react';
import P from '../components/P/P';
import Heading from '../components/Heading/Heading';

const HomePage = () => (
  <div>
    <Heading>Webpack config for server-side rendered React</Heading>
    <P>Boostrap your isomorphic React project</P>
  </div>
);

export default HomePage;
